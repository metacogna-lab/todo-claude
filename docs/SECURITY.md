# SECURITY

- Secrets never land in repo; use env templates + vault references.
- Enforce least privilege across connectors (Obsidian, Todoist, Linear).
- Monitor anomalies via Langfuse security dashboards and browser CSP reports captured in DevTools.
