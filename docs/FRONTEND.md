# FRONTEND

- React + Vite stack with Zustand store; no additional global stores allowed.
- Each feature exposes DevTools recording scripts.
- Services interactions pass through typed hooks generated from schema definitions.
