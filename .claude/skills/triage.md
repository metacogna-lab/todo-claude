# Skill: Triage + Plan

When asked to capture a request, create a deterministic plan:
- One Obsidian note that preserves context
- Todoist tasks for personal action items
- Linear issues only for scoped engineering work

Return a JSON plan that matches the app schema.
