# --- OpenAI ---
# Provide your OpenAI API credentials
OPENAI_API_KEY=your_openai_api_key

# Optional model override (otherwise SDK default)
OPENAI_MODEL=gpt-4o-mini

# --- Obsidian ---
# Local vault mode (recommended): absolute path to your vault root.
OBSIDIAN_VAULT_PATH=/Users/you/Obsidian/MyVault

# Optional: if you use an Obsidian REST plugin (community), point to it instead.
# OBSIDIAN_REST_URL=http://127.0.0.1:27123
# OBSIDIAN_REST_TOKEN=your_token

# --- Todoist REST v2 ---
TODOIST_API_TOKEN=your_todoist_api_token
TODOIST_DEFAULT_PROJECT_ID=
TODOIST_DEFAULT_LABELS=ai,triage

# --- Linear GraphQL ---
LINEAR_API_TOKEN=your_linear_api_token
LINEAR_DEFAULT_TEAM_ID=
LINEAR_DEFAULT_ASSIGNEE_ID=

# --- Langfuse Observability ---
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com

# --- Behavior ---
# When true, the assistant will only produce a plan + receipt (no API writes).
DRY_RUN=false

# Where to write receipts in Obsidian (relative path inside vault)
RECEIPTS_FOLDER=AI/Receipts

# Optional: add stable project tags that will be added to every item created.
GLOBAL_TAGS=personal-assistant,claude,automation
